from __future__ import annotations

"""
Reserved for future scoring utilities that operate purely on domain models.

Phase 1 constraint: `engine/` must not import `providers/` or any HTTP/adapters.
"""
